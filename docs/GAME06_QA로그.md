# 「솜솜 합체」 QA 로그

작성 2026-09-13 · 자동 플레이 기반

---

## 0. 어떻게 했나

Playwright로 브라우저를 띄우고 봇 네 종류에게 판을 시켰습니다. 매 프레임 상태를 읽어 이탈·겹침·점수·종료 여부를 기록했습니다.

| 봇 | 어떻게 두나 | 무엇을 보나 |
|---|---|---|
| `smart` | 같은 단계 중 **가장 위에 있는 것** 바로 위에 정확히 놓는다 | 실력 천장 |
| `human` | 겉으로 드러난 같은 단계만 노리고, 조준이 ±18px 빗나간다. 25%는 빈 자리에 놓는다 | 보통 사람 |
| `random` | 아무 데나 | 바닥 |
| `edge` | 좌우 벽에만 번갈아 붙인다 | 벽 처리 |

`smart`는 파묻힌 조각까지 꿰뚫어 보는 **실존하지 않는 플레이어**입니다. 난이도 기준으로 쓰면 안 되고, 천장을 재는 데만 씁니다.

---

## 1. 물리 — 고친 것

### 1.1 조각이 벽을 뚫고 나감

처음엔 벽 보정 순서 문제라고 보고 반복문 뒤에 벽 보정을 한 번 더 넣었습니다. **틀렸습니다.** 그래도 계속 나갔습니다.

몸마다 `born`(태어난 지 몇 초)과 이탈 방향을 찍어 보니 나간 것들이 전부 `born: 0`이었습니다. **합체로 막 생긴 조각**이었습니다. 부모 둘의 중간 지점에 부모보다 큰 반지름으로 태어나니, 그 자리가 이미 벽 밖이었던 겁니다.

두 군데를 고쳤습니다.

1. 만들 때 바로 통 안으로 가둡니다 (물리 스텝을 기다리면 한 프레임 동안 벽을 뚫고 보입니다)
2. 보정 반복의 순서를 `벽 → 원끼리`에서 **`원끼리 → 벽`**으로 뒤집었습니다. 밀어내기로 끝나면 그 결과가 통 밖에 남은 채 프레임이 끝납니다. 벽 가두기로 끝나면 그럴 수 없습니다

결과: 네 봇 모두 이탈 0.

### 1.2 자리 잡은 조각들이 최대 23px 겹침

보정 반복을 8회에서 14회로 늘리고, 순서를 위와 같이 바꿨습니다. 다만 반복을 늘리자 새 문제가 생겼습니다 — 아래 1.3.

또 하나, 겹침 최대치의 절반쯤은 **합체가 일어난 그 프레임**이었습니다. 새 조각이 제 크기로 즉시 태어나 이웃을 파고든 것이지, 쌓임이 불안정한 게 아니었습니다. 그래서 측정을 둘로 나눴습니다.

| | 전체 최대 겹침 | 자리 잡은 것(생성 0.5초 경과)만 |
|---|---|---|
| 고치기 전 | 28~34px | 14.3px |
| 순서 수정 후 | 28~48px | 7~11px |
| 새 조각 부풀리기 후 | 정상 범위 | 정상 범위 |

새 조각은 **부모 크기로 태어나 0.12초에 걸쳐 제 크기로 부풉니다.** 파고드는 스파이크가 사라지고, 덤으로 합체가 눈에 보이는 연출이 됐습니다.

### 1.3 바닥에서 구르지 않고 박힘

반복 안에서 벽 마찰 `0.86`을 매번 곱하고 있었습니다. 8회일 땐 0.31이라 어찌어찌 굴렀는데, 14회로 올리니 **0.86¹⁴ ≈ 0.12**가 되어 바닥에 닿는 순간 옆으로 못 움직였습니다.

굴러서 빈 자리를 찾아 들어가는 게 이 게임의 재미인데 그게 죽습니다. **위치는 매 반복 잡되, 속도 감쇠는 첫 반복에만** 걸도록 고쳤습니다.

---

## 2. 난이도 — 진짜 문제

### 2.1 아무도 지지 않았다

| 설정 | 봇 | 조각 | 합체 | 통에 남은 것 | 끝났나 |
|---|---|---|---|---|---|
| 기본 (r 15~52) | smart | 200 | 185 | 10 | **아니오** |
| 기본 | random | 200 | 180 | 14 | **아니오** |
| ×1.5 | smart | 200 | 190 | 3 | **아니오** |
| 수박게임 비율 | random | 200 | 181 | 13 | **아니오** |

200개를 부어도 통에는 3~14개만 남습니다. **투하 200번에 합체 180번** — 거의 모든 조각이 놓자마자 합쳐집니다.

### 2.2 조각을 키워 봤더니 오히려 쉬워졌다

크게 하면 통이 빨리 차겠다고 생각했는데 반대였습니다.

보늘이 커지면 **두 번째 보늘이 첫 번째에 반드시 닿습니다.** 통 너비가 보늘 1.5개밖에 안 되니 나란히 놓일 수가 없습니다. 닿으면 소멸 규칙이 발동해 통이 확 비워집니다. 가장 크게 잡은 설정에서 보늘 소멸이 9번 일어나 250개를 버텼습니다.

### 2.3 원인은 사다리 길이다

사다리가 여섯 칸이라 **짝이 안 맞아 남을 수 있는 조각은 람다·끼토·보늘 셋뿐**입니다. 아래 세 단계는 계속 새로 떨어지니 금방 짝이 생깁니다.

기본 크기에서 이 셋이 통에서 차지하는 넓이는 **8%**입니다. 통이 찰 수가 없습니다.

수박게임은 열한 칸이라 짝 없는 중간 단계가 예닐곱 개씩 자리를 차지하고 눌러앉습니다. 그게 그 게임이 끝나는 이유입니다. 우리는 캐릭터가 여섯뿐이라 사다리를 늘릴 수 없습니다.

크기로도, 통 모양으로도 풀리지 않는 **구조의 문제**입니다.

### 2.4 그래서 한 판을 조각 수로 묶었다

> **한 판 = 조각 50개.** (실기 피드백 뒤 60개 + 소멸 보너스로 조정 — 5장)

- 시간 제한이 아닙니다. 시간을 걸면 빨리 누르는 게임이 되고, 그건 낙엽피하기·달리기와 또 겹칩니다
- 모두가 같은 예산을 씁니다. 오래 붙잡고 있는 사람이 이기지 않으니 **가족 랭킹이 그제야 뜻을 갖습니다**
- 넘침은 그대로 둡니다. 막 던지면 50개를 다 못 쓰고 끝납니다

반지름은 **합체 없이 50개를 다 부으면 통이 1.4배 넘치는 크기**로 다시 잡았습니다.

```
R_BASE = [26, 35, 46, 58, 72, 92]   (너비 382px 통 기준)
```

---

## 2.5 점수에 실력이 안 나타났다

판이 끝나게 만든 뒤 봇 셋에게 같은 시드로 한 판씩 시켰습니다.

| 봇 | 점수 | 합체 | 최고 연쇄 | 통에 남은 것 |
|---|---|---|---|---|
| 아무 데나 던지는 봇 | 1280 | 37 | 13 | 13 |
| 사람 흉내 봇 | 1298 | 41 | 5 | 8 |
| 완벽한 봇 | 1354 | 37 | 7 | 12 |

**완벽하게 둬도 6% 차이입니다.** 랭킹을 올려도 순위가 뜻이 없습니다.

원인은 점수 식이었습니다. 조각이 50개로 고정이니 합체 횟수도 37~41로 거의 고정인데, 예전 값(`[1,3,6,12,24,50]` · 연쇄 배수 0.5)에서는 **점수의 5분의 4가 연쇄에서 나왔습니다.** 그리고 연쇄는 굴러가다 우연히 붙는 것이라 대체로 운입니다. 무작위 봇이 13연쇄를 내고 사람 봇이 5연쇄에 그친 게 그 증거입니다.

그래서 눈금을 바꿨습니다.

```
MERGE_SCORE  [1,3,6,12,24,50] → [0,5,15,45,130,400]
POP_SCORE    120 → 800  (연쇄 배수 없이 고정)
연쇄 배수     0.5 → 0.35
```

보늘 소멸에 연쇄 배수를 안 붙인 이유가 있습니다. 처음엔 붙였는데, 11연쇄가 얹힌 소멸 한 번이 3880점이 되어 그 한 번으로 판이 갈렸습니다. 보늘 둘을 붙이는 건 작정하고 만드는 일이지 굴러가다 우연히 터지는 게 아니므로, 배수 없이 고정 800점으로 두었습니다.

### 바꾼 뒤 — 그리고 봇으로는 여기까지가 한계입니다

같은 봇 셋에게 시드 셋으로 아홉 판을 시켰습니다.

| 시드 | 무작위 봇 | 사람 흉내 봇 | 완벽한 봇 |
|---|---|---|---|
| 11 | 4865 | 2611 | 6084 |
| 44 | 4149 | 6413 | 3015 |
| 77 | 5449 | 6458 | 5517 |
| **평균** | **4821** | **5161** | **4872** |

**세 봇의 평균이 사실상 같습니다.** 같은 봇 안에서의 편차(2611~6458)가 봇 사이의 차이보다 큽니다.

바꾼 것이 확실히 나아진 점은 있습니다. **한 판 한 판이 서로 달라졌습니다.** 예전엔 누가 어떻게 두든 1280~1354로 붙어 있었는데 지금은 2600~6500으로 벌어집니다. 랭킹에 최소한 눈에 보이는 차이는 남습니다.

다만 그 차이가 **실력에서 오는지는 봇으로 확인할 수 없었습니다.** `smart` 봇이 쓰는 전략(같은 단계 중 제일 위에 있는 것 위에 놓기)이 실제로 좋은 수가 아니기 때문입니다. 위에 얹으면 아래가 파묻혀서, 통을 정리하는 사람의 수와 다릅니다. **진짜 실력 기울기를 가진 봇을 제가 못 만들었습니다.**

그래서 여기서 멈췄습니다. 믿을 수 없는 잣대에 맞춰 숫자를 더 돌리는 건 튜닝이 아니라 헛수고입니다. **이건 사람이 몇 판 해 봐야 답이 나옵니다.**

만약 해 보니 잘 둔 판과 막 둔 판의 점수가 비슷하다면, 손댈 곳은 `POP_SCORE`(지금 800, 한 판 점수의 15~25퍼센트)와 `CHAIN_MUL`(지금 0.35) 두 줄입니다.

업적 기준도 새 눈금에 맞췄습니다. `merge_chain4`(4연쇄)는 무작위로도 13연쇄가 나와 무의미했고, `merge_1000`은 모든 판에서 열렸습니다.

| 이전 | 이후 |
|---|---|
| `merge_chain4` 4연쇄 | `merge_chain6` 6연쇄 |
| `merge_1000` 1000점 | `merge_4000` 4000점 |

---

## 3. 기기 차이 — 고친 것

반지름이 고정 픽셀이었습니다. 캔버스는 `width:100%`라 기기마다 통 너비가 다른데 조각 크기는 같으니, **좁은 폰일수록 어렵고 태블릿일수록 쉬웠습니다.**

- 조각 반지름을 통 너비에 비례시켰습니다
- 통 높이를 너비의 1.45배로 묶었습니다. 세로로 긴 폰에서 통이 깊어지는 것을 막습니다
- 떨어뜨리는 공간을 `가장 큰 투하 조각의 지름 + 20px`로 계산합니다. 고정값이면 조각 크기를 바꿀 때마다 HUD를 가립니다

| 화면 | 통 | 조각 반지름 |
|---|---|---|
| 420×680 | 382 × 491 | 26 35 46 58 72 92 |
| 320×560 | 292 × 398 | 20 27 35 44 55 70 |
| 520×900 | 474 × 685 | 32 43 57 72 89 114 |

---

## 4. 그 밖에 고친 것

| 증상 | 원인 |
|---|---|
| 미개가 도감에 안 들어감 | 합체로 **생긴** 단계만 기록. 떨어뜨린 단계는 빠짐 |
| 결과 화면 제목이 항상 "골목이 넘쳤다" | 조각을 다 써서 끝난 경우가 없었음. 또 통인데 골목이라고 씀 |
| 새 판에 이전 판의 누적 시간이 넘어옴 | `startGame()`에서 `acc`를 안 비움 |
| 마을 공통 업적이 합체로는 안 열림 | 허브 공통 업적을 합체 종료 시 확인하지 않았음 |
| 손에 든 조각이 넘침선을 파고듦 | 그리는 위치가 `BOX.y - 30` 고정. 조각이 커지자 선 아래로 내려감 |
| **조각을 다 썼는데 판이 안 끝남** | 통이 차면 조각들이 서로 밀며 미세하게 계속 떨려 "완전히 멈춤" 조건이 영영 안 맞음. 조각 17개 남은 판에서 확인. 앞서 4개만 남은 판으로 검증해 놓쳤던 것. 마지막 조각 후 3.5초가 지나면 끝내도록 수정 |

---

## 5. 실기 1차 피드백 뒤 (2026-09-13 오후)

"한 판이 조금 짧다"는 평이 나왔습니다. 봇 데이터로도 50개째에 통이 반밖에 안 차서 마지막 조각을 놓을 때 긴장이 없었습니다.

| 바꾼 것 | 내용 |
|---|---|
| 길이 | 조각 50 → 60. **보늘 소멸마다 +5**. 한 판 길이가 실력에서 나오게 |
| 바꾸기 칸 | 손에 든 조각을 옆 칸과 바꿈. 한 판 3번. 빠른 손이 아니라 계획이 점수가 되게 |
| 표정 | 합체로 태어나면 `_win`, 세게 눌리면 `_hit`. 기존 자산 |
| 오늘의 통 | 날짜 씨앗 난수. 하루 동안 모두 같은 순서. 랭킹 문서에 `d` 필드 추가 |
| 결과 화면 | 통을 가리지 않는 반투명 카드 |
| 그림 | 타이틀 히어로·상점 벽·널빤지 세 장을 멀티샷으로 뽑아 적용. 캐릭터 신규 작화는 여전히 0 |

봇 재측정(같은 시드): 무작위 6565(합체 50, 소멸 1), 완벽 7550(합체 57, 소멸 1). 판이 길어졌고 소멸 보너스가 실제로 붙었습니다.

새로 잡은 것: 바꾸기 칸을 누른 손가락이 떼어질 때 조각이 떨어지는 문제(터치 시작 위치를 기억해 막음), 마지막 조각을 놓은 뒤 소멸이 일어나면 손이 비어 있던 문제(보너스 조각과 함께 손을 다시 채움), 타이틀을 히어로 구성으로 바꾸며 닫는 태그를 빠뜨려 결과 화면이 타이틀 안에 갇혔던 문제(흐름 QA가 잡음).

## 6. v2 (2026-09-13 밤)

기획서 `docs/GAME06_v2_기획서.md`. 판은 그대로 두고 판 바깥을 채웠습니다.

| 기능 | 어떻게 확인했나 |
|---|---|
| 오늘의 순위판 | Firestore 응답을 가짜로 바꿔 넣고: 닉네임별 최고점 정렬, 내 이름 강조, 방금 끝난 내 점수 즉시 반영, 빈 상태 문구, 읽기 실패 문구 |
| 오늘의 규칙 | `FORCE_RULE` 훅으로 여섯 규칙을 강제해 각각 실제 수치 반영 확인. 같은 날 새로고침 시 같은 규칙·같은 순서 |
| 자랑하기 | 헤드리스에서 1080×1350 캔버스 생성, 가운데·점수 자리 픽셀이 비어 있지 않음, `navigator.share` 를 지우면 시트 폴백에 이미지(1080×1350)가 뜸 |
| 연출 | 합체·소멸 뒤 `floats`·`ghosts` 배열이 채워지고 JS 오류 0. 물리+그리기 한 프레임 0.4ms |
| 계절 벽 | 9월이라 `merge_wall_autumn.jpg` 가 로드됨 |
| 회귀 | v1 흐름 QA 전부 통과 |

잡은 것: `draw()` 를 `drawCrate`/`drawWall` 로 쪼개면서 손에 든 조각·바꾸기 칸·규칙 표시 블록이 함수 밖에 남아 문법 오류("Unexpected token }"). 스모크 테스트가 바로 잡았고, 블록을 `draw()` 안으로 되돌렸습니다.

확인 못 한 것: 휴대폰 공유 시트(카카오톡으로 실제 전송). 이미지 생성과 폴백까지만 검증했습니다.

## 7. v2.1 — 연쇄를 어떻게 세느냐가 곧 실력의 정의였다 (2026-09-19)

실기 피드백 "어느 이상 점수가 나기 힘들다"에 맞춰 연쇄 배수를 가파르게 하고(상한 ×9) 과열(4연쇄 → 8초 ×2)과 완벽 정리(+1500)를 넣었습니다.

**그랬더니 무작위 봇이 40159점을 냈습니다.** 최고 연쇄 11. 아무 데나 던지는 봇이 11연쇄를 낼 리가 없습니다.

원인은 연쇄의 정의였습니다. **시간만 겹치면 연쇄로 세고 있었습니다.** 조각 하나를 떨어뜨리면 통 여기저기서 서로 무관한 합체가 동시에 터지는데, 그게 0.75초 창 안에 들어오면 전부 한 줄로 묶였습니다.

합체로 태어난 조각이 끼어 있어야 연쇄로 세도록 고쳤습니다.

```js
function isCascade(a, c){
  return !!((a.rT && a.born < CHAIN_WINDOW) || (c.rT && c.born < CHAIN_WINDOW));
}
```

| 무작위 봇, 보통 날 고정 | 시드 11 | 시드 44 | 최고 연쇄 |
|---|---|---|---|
| v2 (연쇄 배수 0.35) | 3894 | — | 5 |
| v2.1 시간만 겹쳐도 연쇄 | 25885 | 40159 | 9 · 11 |
| v2.1 진짜 연쇄만 | **15306** | **17652** | **4 · 4** |

천장은 올라가고(3894 → 1만5천~1만8천, 약 4배) 운으로 나던 대박은 사라졌습니다(2만6천~4만의 널뛰기가 1만5천~1만8천으로 좁아졌습니다).

연쇄가 배수를 만드는 이상 **연쇄의 정의가 곧 실력의 정의**입니다. 정의를 느슨하게 두면 배수를 아무리 정교하게 설계해도 운 게임이 됩니다.

### 실력 격차는 이번에도 못 쟀습니다

| 진짜 연쇄만, 보통 날 고정 | 시드 11 | 시드 44 |
|---|---|---|
| 무작위 봇 | 15306 | 17652 |
| "완벽한" 봇 | 15770 | **5661** |

완벽한 봇이 시드 44에서 무작위 봇의 3분의 1밖에 못 냈습니다. v1에서 이미 적어 둔 그대로입니다 — **`smart` 봇의 전략(같은 단계 중 제일 위에 얹기)은 실제로 좋은 수가 아닙니다.** 위에 얹으면 아래가 파묻히고, 연쇄를 설계하지도 않습니다. 연쇄 배수를 쟤야 하는데 연쇄를 노릴 줄 모르는 봇이라 잣대가 될 수 없습니다.

그래서 이 변경이 **잘 두는 사람에게 실제로 더 많은 점수를 주는지는 사람이 몇 판 해 봐야** 압니다. 구조상으로는 그렇게 되어 있습니다: 쌍을 미리 쌓아 두고 아래를 터뜨려야 3단 이상 연쇄가 나오고, 그건 그다음 조각까지 보면서 두 수 앞을 계획해야 가능합니다. 봇은 그걸 안 합니다.

격차가 너무 크면 `chainMul`의 이차항(0.1)과 `FEVER_MUL`(2)을 낮추고, 너무 작으면 반대로 올리면 됩니다.

### 그 밖에 잡은 것

- 과열 띠를 통 바로 위에 뒀더니 손에 든 조각과 겹쳤습니다. HUD 아래 알림 줄로 옮기고, 과열 중에는 오늘의 규칙 표시를 접습니다(같은 줄을 씁니다).
- 튜토리얼 한 줄이 길어져 화면 밖으로 나갔습니다. 폭에 맞을 때까지 글자를 줄입니다.
- 점수 검증 중 "연쇄 0인데 1505점"이 나왔는데, **테스트가 틀린 것**이었습니다. 조각에 `id:1,2`를 직접 박았더니 게임의 `nextId`와 충돌해 합체로 생긴 조각이 같이 지워졌고, 통이 비어 완벽 정리가 발동했습니다. 게임 코드는 정상이었습니다.

## 8. 남은 것

- **Firestore 규칙에 `merge_scores`·`merge_progress` 추가**가 필요합니다. 현재 규칙은 컬렉션 허용목록 방식이라 새 컬렉션은 403입니다. 막혀 있어도 게임은 정상이고 랭킹만 안 올라갑니다
- **조각 60개 + 소멸 보너스 5**는 1차 실기 피드백으로 맞춘 값입니다. 다시 조정할 땐 `DROPS_MAX`·`POP_BONUS_DROPS` 두 줄입니다.
- 손맛(반발 0.12, 마찰 0.86)은 실기에서 봐야 합니다


## 9. v3 한가위 업데이트 QA (2026-09-23)

| 항목 | 방법 | 결과 |
|---|---|---|
| 이벤트 기간 안 (9/25로 고정) | `window.FORCE_DAY` | 띠 표시, 보름달 벽, 소멸 1.5배(퍼즐은 1배), 2000점 넘을 때마다 컷 1장씩 3장, 넷째 판부터 안 나옴, `merge_chuseok` 업적, 앨범에 한가위 묶음 |
| 이벤트 기간 밖 (10/10) | 같음 | 띠 숨김, 가을 벽, 배율 1, 컷 안 나옴, 새로워진 것 카드에서 한가위 줄 숨김 |
| 새로워진 것 카드 | 판수 2인 사람 / 처음 온 사람 | 한 번만 뜸, 새로고침하면 안 뜸 / 처음 온 사람에겐 안 뜸 |
| 상자 꾸미기 | 잠긴 상자 누르기, 조건 채우고 고르기 | 잠긴 건 안내만, 고른 상자 저장·새로고침 유지, 네 개 다 열리면 `merge_skins` 업적(같은 판에서 바로) |
| 타이틀 배치 | 320×560 ~ 520×900 다섯 크기 | 버튼 넘침 없음, 가로 스크롤 없음 |
| 허브 테마곡 | 첫 탭, 버튼 끄기/켜기, 같은 세션 새로고침 | 첫 탭에 한 번 재생, 끄면 공용 음소거 키 저장, 같은 세션에선 다시 안 틂 |
| 회귀 | smoke / qa_flow / qa_hub | 모두 통과, JS 오류 0 |

잡은 것: 네 상자 업적이 판 끝 집계(`lifeAdd`)보다 허브 보고가 먼저 돌아 한 판 늦게 열리던 것을 `lifeAdd` 안에서도 확인하도록 고쳤습니다.

남은 것: Firestore 규칙에 `merge_puzzle` 블록을 붙여야 퍼즐 순위가 올라갑니다. 막혀 있어도 퍼즐은 "내 기록"으로 정상 동작합니다.

## 10. v3.1 한 판 길이·규칙 균형 (2026-09-23)

빠른 자동 플레이(페이지 안에서 물리를 직접 돌려 한 판 1~2초)로 규칙마다 8판씩 쟀습니다. 봇은 겉에 보이는 같은 친구를 75% 확률로 노리고, 조준은 흔들립니다.

| 규칙 | 평균 점수 | 보통 날 대비 | 놓은 조각 | 넘침 |
|---|---|---|---|---|
| 보통 날 | 15,419 | — | 91.9 | 0/8 |
| 과열이 긴 날 | 16,148 | +4.7% | 91.9 | 0/8 |
| 보늘이 후한 날 | 16,060 | +4.2% | 99.0 | 0/8 |
| 할머니가 많은 날 | 14,894 | −3.4% | 74.5 | 0/8 |
| 미개가 많은 날 | 14,458 | −6.2% | 105.6 | 0/8 |
| 통이 얕은 날 | 14,372 | −6.8% | 91.2 | 0/8 |

바꾸기 전(조각 60개, 크기 1배): 보통 10,372 / 할머니 12,172(+17%) / 미개 7,860(−24%).

오늘의 퍼즐(작아진 크기): 평균 별 2.25개, 오류 없음. 회귀 smoke·qa_flow 통과, JS 오류 0.

## 11. v3.2 긴장감·한 판 더 (2026-09-23)

### 높이 배수 조정 과정 (보통 날, 봇 8판)

| 설정 | 평소대로 둠 | 일부러 높이 쌓음 | 넘침 |
|---|---|---|---|
| 배수 35~85%·최대 2.5, 통 그대로 | 18,600 | 26,574 (+43%) | 0 |
| 같은 배수, 통 0.62배 | 26,250 | 30,735 | 1/8 |
| 배수 50~92%·최대 2, 통 너비×1.15, 판정 0.7초 (채택) | 21,401(16판) | 23,792~29,666 | 0 |

처음 설정은 쌓기만 하면 점수를 공짜로 얹어 주고 위험이 없었습니다. 채택안은 평소 점수 인상이 작고, 일부러 쌓는 사람이 확실히 더 받습니다.

### 규칙별 (채택안, 12~16판)

| 규칙 | 평균 | 보통 날 대비 | 넘침 |
|---|---|---|---|
| 보통 날 | 21,401 | — | 0/16 |
| 과열이 긴 날 | 22,844 | +7% | 0/16 |
| 보늘이 후한 날(+7) | 22,327 | +4% | 0/12 |
| 할머니가 많은 날(74개) | 21,828 | +2% | 0/12 |
| 미개가 많은 날 | 21,356 | 0% | 0/12 |
| 통이 얕은 날 | 19,588 | −8% | **4/12** |

### 그 밖에

- 통 깊이가 기기마다 달랐음(320×560은 너비의 1.4배, 390×844는 1.9배) → 너비의 1.15배로 고정해 모든 기기에서 같게
- 320×560에서 결과 카드 윗부분(제목·점수)이 잘림 → 결과 화면을 스크롤되게, 작은 화면에서 컷 그림 150px
- 배수 눈금 글자가 캐릭터를 가림 → 캐릭터 뒤에 그림
- 오늘의 퍼즐: 평균 별 2.33, 오류 없음. 회귀 qa_flow·qa_v3 통과, JS 오류 0
