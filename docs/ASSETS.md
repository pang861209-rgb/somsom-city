# 재활용 자산 목록

작성 2026-09-13 · 전체 559개 파일, 112MB

신작을 기획할 때 "무엇을 새로 그려야 하는가"를 판단하기 위한 목록입니다. 결론부터 말하면 **캐릭터와 배경은 새로 그릴 게 거의 없습니다.**

## 1. 캐릭터 스프라이트 — 36장

`assets/sd_<이름>[_변형][_t].png`

여섯 캐릭터 × 표정 3종(기본 · hit · win) × 2판본(일반 · `_t` 투명)입니다.

| 캐릭터 | 파일 키 | 비고 |
|---|---|---|
| 끼토 | `kkito` | 주인공 |
| 보늘 | `boneul` | 가장 큼 (끼토의 110%) |
| 미개 | `migae` | 가장 작음 (끼토의 41%) |
| 람다 | `ramda` | |
| 북거씨 | `bookgeo` | |
| 라칠친 할머니 | `lachilchin` | 허브 코드에서는 `lachil`로도 참조 |

아케이드 게임에 바로 쓸 수 있는 유일한 완결 세트입니다. 낙엽피하기·달리기가 이미 이걸 씁니다.

**표정 3종은 좁습니다.** 새 게임에서 감정 연출이 필요하면 `놀람`, `지침`, `기쁨` 정도를 추가하는 게 우선순위 1번입니다. 여섯 캐릭터 × 3표정 = 18장이면 됩니다.

## 2. 캐릭터 보드 — 6장

`assets/board_<이름>.png` (1400×1045 내외)

작가 원화 수준의 3면도 + 표정 시트입니다. **삽화를 새로 생성할 때 참조로 넣는 원본**입니다. 게임에 직접 쓰지는 않습니다.

토큰 절약을 위해 768px로 줄인 사본을 쓰는 규칙이 `tools/README.md`에 있습니다.

## 3. 삽화 — 약 500장

### 3-1. 「끼토의 하루」 계절 컷 — 202장

`assets/day/` 아래, 접두사로 계절이 갈립니다.

| 접두사 | 계절 | 장수 |
|---|---|---|
| `r_` | 가을 · 솜재페 | 42 |
| `w_` | 겨울 · 등불 밤 | 49 |
| `p_` | 봄 · 벼룩장 | 57 |
| `u_` | 여름 · 옥상 영화제 | 54 |

전부 3:4 세로 삽화입니다. 대부분 한 장면에 캐릭터 1~3명과 배경이 들어 있습니다.

**신작에서의 용도: 사건 카드, 해금 보상, 도감.** 글 없이 그림만 봐도 상황이 읽히는 컷이 많아 카드로 쓰기 좋습니다.

### 3-2. 막간극 컷 — 33장

`i1_` 8 · `b1_` 6 · `b2_` 6 · `b3_` 6 · `b4_` 7

다른 캐릭터 시점의 하루입니다. 캐릭터별 개성이 강해 **캐릭터 해금 카드**로 적합합니다.

### 3-3. 결말 컷 — 8장

`assets/ending_e0`~`e7`. 896×1200. 완성도가 높은 편입니다.

`e1_pizza`는 2026-09-13에 끼토 캐논 불일치로 재생성했습니다.

### 3-4. 기타

`sp_` 18 · `fs_` 13 · `dex_` 8 · `special_` 6 · `frame_` 24 · `intro_` 18 · `prop_` 5 · `title_` 3

## 4. 배경 — 35장

### 4-1. 장소 배경 13장 (`assets/bg_*.png`)

골목 · 901호 현관 · 작업방 · 보늘 집 · 대나무숲 빌라 입구 · 도토리 상점 · 향수 공방 · 할머니 거실 · 시티청 · 장터 · 축제 · 비 오는 골목 · 달리기 트랙

### 4-2. 계절·시간 변형 22장 (`assets/day/bg_*.jpg`)

골목과 901호 현관, 두 장소를 각각 11가지 상태로 그려 둔 세트입니다.

`dusk` · `ginkgo` · `green` · `heat` · `may` · `night` · `petal` · `rain` · `spring` · `summer` · `winter`

**같은 장소의 계절 변화가 이미 있다는 건 큽니다.** 신작에서 스테이지 배경을 돌려 쓸 때 새로 안 그려도 됩니다.

## 5. 짝맞추기 카드 — 65장

`assets/card_<이름>_<번호>.png` + `card_back.png`

| 캐릭터 | 장수 |
|---|---|
| 끼토 · 미개 · 람다 · 라칠친 | 각 12 |
| 보늘 · 북거씨 | 각 8 |

GAME 03이 씁니다. 정사각 아이콘 형태라 **신작의 아이템·능력 아이콘으로 전용 가능**합니다. 새로 그릴 필요가 거의 없습니다.

## 6. 코드 자산

| 자산 | 위치 | 재활용 가치 |
|---|---|---|
| 허브 연동 규격 | `index.html` | 도토리·업적·도감·미션·주간 랭킹. 신작은 이 규격만 맞추면 붙습니다 |
| 랭킹 제출 패턴 | `leaf/`, `run/`, `match/` | Firestore 컬렉션 하나 추가하면 끝 |
| 장면 엔진 | `day/index.html` | 컷 표시·선택지·효과 적용. 덜어내면 카드 게임에 씁니다 |
| 삽화 생성 도구 | `tools/` | 캐논 규칙과 토큰 절약 규칙이 문서화돼 있습니다 |
| 세계관 | `day/STORY_BIBLE.md` | 네 계절 분량의 설정과 관계 |

## 7. 새로 그려야 할 것 (추정)

| 항목 | 장수 | 우선순위 |
|---|---|---|
| 스프라이트 표정 추가 (놀람·지침·기쁨) | 18 | 높음 |
| 신작 전용 UI 아이콘 | 10~15 | 높음 |
| 신작 타이틀·튜토리얼 컷 | 3~5 | 중간 |
| 스테이지 배경 (기존으로 부족할 경우) | 0~6 | 낮음 |

**합계 30~45장.** 559장을 이미 갖고 시작하는 것치고는 적은 편입니다.

## 8. 주의

- **캐릭터는 여섯뿐입니다.** 새 인물이나 동물을 만들지 않습니다.
- 캐논 우선순위는 작가 원화 > 소설 ep1 > 비주얼 바이블 > 문서입니다.
- 자주 틀리는 캐릭터 규칙(끼토 귀·체형, 미개 크기, 북거씨 목, 볼터치 금지 등)은 `tools/README.md`에 정리돼 있습니다. 삽화를 새로 뽑기 전에 반드시 읽어야 합니다.
